---
title: "도커에 대해 모든 것"
category: 'Tech'
subcategory: 'Cloud Programming'
date: "2026-03-31"
coverImage: "/images/docker/docker.png"
description: "이번 블로그로 도커 및 컨테이터가 무엇인지, 왜 필요한지 또한 어떻게 사용하는지에 대해 알아볼 것입니다"
---

# 컨테이너이란 무엇인가?


# 콘테이터가 어떤 문제를 해결해주는가
우리는 웹 서버, 이메일 서버, 데이터베이스에서 구성된 어떤 앱을 운영한다고 가정해보자. 이 때 IaaS 클라우드 컴퓨팅 서비스를 사용하면 다음과 같이 3개의 가상 머신이 필요할 것입니다. 

각 컴포넌트가 별도의 VM에서 실행

<div style={{
  border: '1.5px dashed #b4b2a9',
  borderRadius: '16px',
  padding: '32px 24px 24px',
  background: '#f8f7f4',
  position: 'relative',
  marginBottom: '12px',
}}>
  <span style={{
    position: 'absolute',
    top: '-13px',
    left: '24px',
    background: '#f8f7f4',
    padding: '0 10px',
    fontSize: '13px',
    fontWeight: '500',
    color: '#5f5e5a',
    letterSpacing: '0.02em',
  }}>☁️ Cloud Server</span>

  <div style={{
    display: 'grid',
    gridTemplateColumns: 'repeat(3, 1fr)',
    gap: '16px',
  }}>
    {[
      { icon: '🌐', label: 'Web Server', color: '#e6f1fb', border: '#85b7eb', text: '#0c447c' },
      { icon: '📧', label: 'Email Service', color: '#e6f1fb', border: '#85b7eb', text: '#0c447c' },
      { icon: '🗄️', label: 'Database', color: '#e6f1fb', border: '#85b7eb', text: '#0c447c' },
    ].map(({ icon, label, color, border, text }) => (
      <div key={label} style={{
        border: `1px solid ${border}`,
        borderRadius: '12px',
        overflow: 'hidden',
        background: '#fff',
        boxShadow: '0 1px 4px rgba(0,0,0,0.06)',
      }}>
        <div style={{
          background: color,
          padding: '10px 12px',
          fontSize: '12px',
          fontWeight: '600',
          color: text,
          borderBottom: `1px solid ${border}`,
        }}>
          VM — Ubuntu OS (2GB)
        </div>
        <div style={{
          padding: '16px 12px',
          textAlign: 'center',
        }}>
          <div style={{ fontSize: '28px', marginBottom: '6px' }}>{icon}</div>
          <div style={{ fontSize: '13px', fontWeight: '600', color: '#2c2c2a' }}>{label}</div>
        </div>
      </div>
    ))}
  </div>

  <p style={{
    marginTop: '16px',
    marginBottom: '0',
    fontSize: '13px',
    color: '#5f5e5a',
    background: '#fff3cd',
    border: '1px solid #e8c87a',
    borderRadius: '8px',
    padding: '10px 14px',
  }}>
    ⚠️ <strong>문제:</strong> OS 3개가 각각 2GB씩 메모리를 차지 — 실제 앱보다 OS 유지에 더 많은 자원이 소모됩니다.
  </p>
</div>

---








